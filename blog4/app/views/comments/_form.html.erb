
<%= form_with(model: comment,url:blog_comments_path(params[:blog_id]))  do |form| %>
  <% if comment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
        <% comment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :blog_id, style: "display: none" %>
    <%= form.text_field :blog_id ,class: "form-control", style: "display: none" %>
  </div>

  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.text_area :content ,class: "form-control"%>
  </div>

  <div>
    <%= form.submit"Add this comment" ,class: "btn btn-primary"%>
  </div>
<% end %>
