
<!--怎么理解_blog中的可以直接使用blog-->
<%= render @blog %>
<hr/>

<% if @blog.user == @current_user %>
  <a href="<%= edit_blog_path(@blog) %>" class="btn btn-primary">Edit</a>
<% else %>
  <a href="<%= edit_blog_path(@blog) %>" class="btn btn-primary not_edit" >Edit</a>
<% end %>
<%= link_to 'Back', blogs_path, class: "btn btn-primary" %>
<hr/>
<% if @blog.user == @current_user %>
  <%= button_to 'Destroy', @blog, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger" %>
<%end %>
<hr/>


<h2>Comments</h2>
<!--<h2>Comments</h2>-->
<!--<div id="comments">-->
  <%#= render @blog.comments %>
<!--</div>-->

<%# @comments.each do |comment| %>
  <%#= render blog_comment_path(2) %>
<!--  <p>-->
    <%#= link_to "Show this comment", blog_comment_path(comment) %>
<!--  </p>-->
<%# end %>
<div id="comments">
  <%= render @blog.comments %>
</div>
<div class="row">
  <%= form_for [@blog, Comment.new]  , id: 'new_comment' do |form| %>
<%#= form_for comment ,remote: true , id: 'new_comment' do |form| %>
    <div class="mb-3">
      <%#= form.label :content, class: "form-label" %>
      <%= form.text_area :content, class: "form-control ",value: "说些什么" %>
    </div>
    <div class="mb-3">
      <%= form.submit "Add Comment", class: "btn btn-primary" %>
    </div>
  <% end %>
</div>
